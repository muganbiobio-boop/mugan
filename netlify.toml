# Netlify — sitio estático Mugan Academia Taekwon-Do ITF
[build]
  command = "npm run build"
  publish = "dist"

# Assets versionados por Vite: cache inmutable
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
